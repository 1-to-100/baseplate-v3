/**
 * List constants
 * List subtype enum and defaults for list creation
 */

import { ListSubtype } from '../types/list';

export { ListSubtype };

/** Default subtype when creating a new list (company). */
export const DEFAULT_LIST_SUBTYPE = ListSubtype.COMPANY;
